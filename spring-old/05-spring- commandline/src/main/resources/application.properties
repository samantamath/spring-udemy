# use wildcard "*" to expose all endpoints or use individual endpoints with comma-delimited list
management.endpoints.web.exposure.include= *
#management.endpoints.web.exposure.exclude=health,info
management.info.env.enabled=true
info.app.name=first app
info.app.description=sam's first app!!
info.app.version=1.0.0

